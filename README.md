# Java-graphs-binary-search-tree
Java implementations of graph data structures using adjacency matrix and adjacency list representations, alongside a binary search tree. Supports directed and undirected weighted graphs with edge operations, degree calculation, and path checking. The BST supports insert, delete, search, in-order traversal, min/max, and successor/predecessor.
